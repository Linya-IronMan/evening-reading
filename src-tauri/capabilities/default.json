{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for evening-reading main window",
  "windows": ["main", "*"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default"
  ]
}
